//! End-to-end tests live in `tests/`; this library exists only to give them a
//! package to belong to.
//!
//! It is deliberately empty. The fixtures in `tests/runtime/` are the tests, and
//! `tests/fixtures.rs` is the harness that runs them.

#![forbid(unsafe_code)]
#![warn(missing_docs)]

-r requirements.txt

# --- Test ---------------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
freezegun==1.5.1                # grant expiry and cooling-off period tests
respx==0.22.0                   # provider contract tests against recorded payloads
hypothesis==6.123.2             # ledger property tests (docs/09 §3.6)

# --- Quality ------------------------------------------------------------------
ruff==0.8.6
mypy==1.14.1
import-linter==2.1              # enforces the layering contract (docs/01 §3)

# --- Security -----------------------------------------------------------------
pip-audit==2.7.3
detect-secrets==1.5.0

# --- Types --------------------------------------------------------------------
types-redis==4.6.0.20241004
pandas-stubs==2.2.3.241126
